apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "keda-msk.fullname" . }}-test-connection"
  labels:
    {{- include "keda-msk.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "keda-msk.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
